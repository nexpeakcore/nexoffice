---
"@betteroffice/xlsx": patch
"@betteroffice/rust-crates": patch
---

The cells an array formula filled belong to it. Typing into one is refused rather than torn out from under the formula, and inserting, deleting or sorting across a spilled result is refused the same way. Replacing the formula itself is allowed and takes the result with it.
